apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  chart:
    spec:
      version: "3.2.1"
  test:
    enable: false
  values:
    ingress:
      hosts:
        - host: podinfo.localhost
          paths:
            - path: /
              pathType: ImplementationSpecific
